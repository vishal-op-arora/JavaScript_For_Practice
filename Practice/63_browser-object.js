/**
 * Browser Object Model
 * Document Object Model (DOM)
 * Windows Object Model
 */
/**
 * Window,Navigator ,History and Location all are the global objects provided by the browser for a web page.
 */
/**
 * Windows Object Model is called "Gloabl Object"
 */

console.log(window);

console.log(window.alert("Hello, Chrome !!!"));

/**
 * all the variable will get created for window object
 */

var x = 1;
console.log(window.x);

window.y = 3;
console.log(window.y);